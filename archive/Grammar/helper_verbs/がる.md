# がる

`がる` is used as a helper verb when you want to talk about what you think someone else `might` feel, want, etc. In Japanese you cannot use something like `ほしい` directly with someone else as you cannot make definitive statements about something you dont definitivly know like someone elses feelings or wants.

`がる` is appended by replacing the training `い` of an adjective.

* `ほしい` --> `ほしがる`
* `こわい` --> `こわがる`

`さくらさんが　ケーキを　ほしがる` (Sakura wants cake)
